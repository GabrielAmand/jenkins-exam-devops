apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: {{ .Values.nginx.service.type }}
  ports:
    - port: {{ .Values.nginx.service.port }}
      targetPort: 8080
      nodePort: {{ .Values.nginx.service.nodePort }}
      protocol: TCP
      name: http
  selector:
    app: nginx