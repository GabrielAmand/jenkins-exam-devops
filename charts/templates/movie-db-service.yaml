apiVersion: v1
kind: Service
metadata:
  name: movie-db
spec:
  type: {{ .Values.movieDb.service.type }}
  ports:
    - port: {{ .Values.movieDb.service.port }}
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: movie-db